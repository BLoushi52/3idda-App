import 'package:flutter/material.dart';

class UserRentals extends StatelessWidget {
  const UserRentals({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'My Rentals',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
