import 'package:flutter/material.dart';

class UserEquipment extends StatelessWidget {
  const UserEquipment({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'My Equipment',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
